<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Store" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body class="bg-blue-100 min-h-screen flex flex-col">
    <nav class="bg-white shadow-md mt-6 p-4 flex items-center justify-between rounded-lg mx-auto max-w-6xl w-full">
      <h1 class="text-2xl font-bold text-gray-800">Welcome to the Rails Marketplace!</h1>
      
      <div class="flex items-center space-x-4">
        <%= link_to "Home", root_path, class: "text-blue-600 hover:text-blue-800 font-medium" %>
        <% if authenticated? %>
          <%= link_to "Profile", user_path, class: "bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition" %>
          <%= button_to "Log out", session_path, method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-700 transition" %>
        <% else %>
          <%= link_to "Login", new_session_path, class: "bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-700 transition" %>
        <% end %>
      </div>
    </nav>

    <% if notice %>
      <div class="bg-green-100 text-green-700 text-center p-4 my-4 mx-auto max-w-6xl rounded-lg shadow">
        <%= notice %>
      </div>
    <% end %>

    <main class="bg-white shadow-lg rounded-lg p-6 max-w-6xl mx-auto my-8 w-full">
      <%= yield %>
    </main>
  </body>
</html>
